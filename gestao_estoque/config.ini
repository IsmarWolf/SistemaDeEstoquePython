[Settings]
default_theme = dark
export_path = ./exports
backup_path = ./backups
low_stock_percentage = 30
inactivity_days = 20

[EmailSettings]
# Configurações para envio de e-mails de notificação (usando Gmail como exemplo)
# IMPORTANTE: Use uma "Senha de App" gerada pelo Google, não sua senha normal.
# Configure estas variáveis como variáveis de ambiente por segurança
sender_email = 
sender_password = 
smtp_server = 
smtp_port = 